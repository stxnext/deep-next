[tool.poetry]
name = "deep_next_common"
version = "0.1.0"
description = "Common tools."
authors = []
packages = [{ include = "deep_next", from = "." }]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
